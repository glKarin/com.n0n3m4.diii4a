DIII4A, com.n0n3m4.diii4a, DOOM III for Android, 毁灭战士3安卓移植版
	
----------------------------------------------------------------------------------

目录:
	assets: 图标资源+OpenGL着色器源码.
	DIII4A: DOOM3前端启动器源码.
	Q3E: DOOM3前端启动器依赖, 包括虚拟按键层, Native渲染层.
	doom3: DOOM3源码, 没有直接创建Native Activity. 而是渲染到Q3E的GLSurfaceView.
	
----------------------------------------------------------------------------------

	__HARAMTTAN__: 经过修改的源码
	__HARAMTTAN__/doom3: 修改过的DOOM3源码, 支持clang++编译. 默认编译arm 32位库.
	__HARAMTTAN__/build: 编译的armv7 32位库.
	__HARAMTTAN__/build/vfp: 编译的armv7 32位库, 硬浮点vfp.
	__HARAMTTAN__/build/neon: 编译的armv7 32位库, 软浮点neon.
	__HARAMTTAN__/DIII4A: 修改过的DOOM3前端启动器源码.
	__HARAMTTAN__/Q3E: 修改过的DOOM3前端启动器依赖的源码.
	
----------------------------------------------------------------------------------

更新:
	* 新增选择OpenGL配置, RGBA8888 32位, RGBA4444 16位, RGB565 16位.
	* 新增选择后台行为, 当前Activity不活动时不调用GLSurfaceView.onPaused/onResume, 不会出现之前的游戏切换到后台, 再回来时黑屏. 也可选择后台继续播放声音.
	* 新增左上角渲染当前内存使用情况, Native堆内存/图形显存使用量. 不需要则不要开启, 操作耗时.
	* 新增额外3个虚拟按键. 原来键盘键, 1,2,3键默认固定到布局.
	* 新增选择OpenGL顶点缓冲区清理行为, 以防止打开光影后, 显存使用量一直增加而最终导致显存OOM, 以致程序闪屏崩溃. 需要使用__HARMATTAN__/build编译的库.
	* DOOM3新增r_harmClearVertexBuffer参数(源码 renderer/VertexCache.cpp)来控制OpenGL顶点缓冲区的清理行为, 0 不清理(原来的行为), 1 每一帧绘制完释放顶点缓冲区内存(默认), 2 每一帧绘制完或关闭渲染系统时释放顶点缓冲区内存. 可以防止打开光影后, 显存使用量一直增加而最终导致显存OOM, 无法申请到显存而导致的程序闪屏崩溃.
	
----------------------------------------------------------------------------------

TODO:
	* DOOM3编译64位版本运行报错. 原来源码中部分都以long型替换了int, 可以存储64位系统的内存地址.