# library-suffix

Simple library that helps to deal with Xash3D FWGS library suffix.

Includes single-header library to detect platform by the predefined
macros (see `build.h`) and the library itself (see `library_suffix.h`).

License is unlicense/public domain/CC-0.
