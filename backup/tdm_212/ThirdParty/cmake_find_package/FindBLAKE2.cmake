include(${CMAKE_CURRENT_LIST_DIR}/tdm_find_package.cmake)

set(BLAKE2_FOUND 1)
set(BLAKE2_INCLUDE_DIRS "${ARTEFACTS_DIR}/BLAKE2/include")
set(BLAKE2_LIBRARY_DIR "${ARTEFACTS_DIR}/BLAKE2/lib/${PACKAGE_PLATFORM}")
if(MSVC)
	set(BLAKE2_LIBRARIES "${BLAKE2_LIBRARY_DIR}/BLAKE2.lib")
else()
	set(BLAKE2_LIBRARIES "${BLAKE2_LIBRARY_DIR}/libBLAKE2.a")
endif()
